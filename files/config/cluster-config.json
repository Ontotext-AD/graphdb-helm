{
  "electionMinTimeout": 500,
  "electionRangeTimeout": 2000,
  "heartbeatInterval": 500,
  "messageSize": 64,
  "verificationMs": 1500,
  "nodes": [
    {{- range $i, $node_index := untilStep 1 (add1 $.Values.graphdb.clusterConfig.nodesCount | int ) 1 }}
    "graphdb-node-{{ $node_index }}:7300"{{- if gt ($.Values.graphdb.clusterConfig.nodesCount | int) $node_index }},
    {{- end}}
    {{- end }}
  ]
}
