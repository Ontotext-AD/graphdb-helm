{{- if and .Values.security.enabled (not .Values.initialConfiguration.users.existingConfigmap) }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "graphdb.fullname.configmap.users" . }}
  labels:
    {{- include "graphdb.labels" . | nindent 4 }}
  {{- with .Values.annotations }}
  annotations:
    {{- tpl ( toYaml . ) $ | nindent 4 }}
  {{- end }}
data:
  {{ .Values.initialConfiguration.users.configmapKey }}: |-
    {{- tpl ( .Files.Get "files/config/users.js" | trim ) . | nindent 4 }}
{{- end }}
