{{ if eq .Values.graphdb.topology "1m_3w" }}
apiVersion: {{ .Values.versions.job }}
kind: Job
metadata:
  name: "1m-3w-test"
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: test-gdb-1m-3w
          image: {{ .Values.images.graphdb }}
          imagePullPolicy: {{ .Values.deployment.imagePullPolicy }}
          volumeMounts:
            - name: graphdb-test-utils
              mountPath: /tmp/utils
          command: [ 'sh', '-c' ]
          args: ["cp /tmp/utils/nodeStatus.sh /usr/local/bin/nodeStatus.sh && cp /tmp/utils/grepFile.sh /usr/local/bin/grepFile.sh; chmod +x /usr/local/bin/nodeStatus.sh && chmod +x /usr/local/bin/grepFile.sh; /usr/local/bin/nodeStatus.sh {{ .Values.graphdb.masters.repository }}"]
      volumes:
        - name: graphdb-test-utils
          configMap:
            name: graphdb-tests-configmap
  {{ end }}