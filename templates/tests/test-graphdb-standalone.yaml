{{ if eq .Values.graphdb.topology "standalone" }}
apiVersion: {{ .Values.versions.job }}
kind: Job
metadata:
  name: "standalone-test"
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: test-gdb-standalone
          image: {{ .Values.images.graphdb }}
          imagePullPolicy: {{ .Values.deployment.imagePullPolicy }}
          volumeMounts:
            - name: graphdb-test-utils
              mountPath: /tmp/utils
          command: [ 'sh', '-c' ]
          args: ["cp /tmp/utils/healthCheck.sh /usr/local/bin/healthCheck.sh; chmod +x /usr/local/bin/healthCheck.sh; source /usr/local/bin/healthCheck.sh ; performHealthCheck {{ .Values.graphdb.masters.repository }} >> /proc/1/fd/1"]
      volumes:
        - name: graphdb-test-utils
          configMap:
            name: graphdb-tests-configmap
  {{ end }}